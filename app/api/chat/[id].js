// export default function handler(req, res) {
//     const { id } = req.query;
//     // You can now use the `id` to perform operations, like fetching chat data,
//     // or working with the PDF using your `pdfChunker` utility.
  
//     // Example: Send a response back with the requested ID
//     res.status(200).json({ chatId: id });
//   }